name: Get latest personal data

on:
  workflow_dispatch:
  push:
  schedule:
    - cron:  '41 1,7,13 * * *'
    - cron:  '41 19 * * *'

jobs:
  scheduled:
    runs-on: ubuntu-latest
    steps:
    - name: Check out repo
      uses: actions/checkout@v2
    - name: Set up Python
      uses: actions/setup-python@v1
      with:
        python-version: 3.9
    - uses: actions/cache@v2
      name: Configure pip caching
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-
    - name: Install Python dependencies
      run: |
        pip install -r requirements.txt
    - name: Get latest data
      run: |
        curl https://systemapi.prod.ashish.me/movies | jq . > movies.json
        curl https://systemapi.prod.ashish.me/shows | jq . > shows.json
        curl https://systemapi.prod.ashish.me/books | jq . > books.json
        curl https://systemapi.prod.ashish.me/todos | jq . > todos.json
        wget https://ashishdotme-downloads.s3.eu-west-1.amazonaws.com/export.zip
    - name: Commit and push if s changed
      run: |-
        git config user.name "Automated"
        git config user.email "actions@users.noreply.github.com"
        git add -A
        timestamp=$(date -u)
        git commit -m "Latest data: ${timestamp}" || exit 0
        git push
    - name: Create db
      run: |
        sqlite-utils insert ashish.db movies movies.json --pk=id
        sqlite-utils insert ashish.db shows shows.json --pk=id
        sqlite-utils insert ashish.db books books.json --pk=id
        sqlite-utils insert ashish.db todos todos.json --pk=id
        healthkit-to-sqlite export.zip health.db
        sqlite-utils drop-table ashish.db rActivityEnergyBurned
        sqlite-utils drop-table ashish.db rAppleExcerciseTime
        sqlite-utils drop-table ashish.db rAppleStandHour
        sqlite-utils drop-table ashish.db rAppleStandTime
        sqlite-utils drop-table ashish.db rAppleWalkingSteadiness
        sqlite-utils drop-table ashish.db rBasalEnergyBurned
        sqlite-utils drop-table ashish.db rBodyMass
        sqlite-utils drop-table ashish.db rDistanceWalkingRunning
        sqlite-utils drop-table ashish.db rFlightsClimbed
        sqlite-utils drop-table ashish.db rHeadphoneAudioExposure
        sqlite-utils drop-table ashish.db rHeartRate
        sqlite-utils drop-table ashish.db rHeartRateVariabilitySDNN
        sqlite-utils drop-table ashish.db rHeight
        sqlite-utils drop-table ashish.db rHKDataTypeSleepDurationGoal
        sqlite-utils drop-table ashish.db rMindfulSession
        sqlite-utils drop-table ashish.db rRespiratoryRate
        sqlite-utils drop-table ashish.db rRestingHeartRate
        sqlite-utils drop-table ashish.db rSixMinuteWalkTestDistance
        sqlite-utils drop-table ashish.db rVO2Max
        sqlite-utils drop-table ashish.db rWalkingAsymmetryPercentage
        sqlite-utils drop-table ashish.db rWalkingDoubleSupportPercentage
        sqlite-utils drop-table ashish.db rWalkingHeartRateAverage
        sqlite-utils drop-table ashish.db rWalkingSpeed
        sqlite-utils drop-table ashish.db rWalkingStepLength
        sqlite-utils drop-table ashish.db workout_points
        sqlite-utils drop-table ashish.db workouts

    - name: Deploy to Vercel
      env:
        VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
      run: |-
        datasette publish vercel ashish.db health.db \
          --token $VERCEL_TOKEN \
          --project ashish-db \
          --title "data.ashish.me" \
          --about "personal database" \
          --about_url "https://github.com/ashishdotme/data.ashish.me" \
          --install datasette-graphql \
          --install datasette-block-robots \
          --install datasette-pretty-json \
          --template-dir "templates"